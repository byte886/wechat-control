# 微信助手（wechat-control）产品需求文档（PRD）

> 版本：v1.0
> 日期：2026-09-10
> 状态：第一阶段已完成，第二/三阶段进行中

---

## 一、产品概述

### 1.1 背景

微信已成为中国人最主要的通讯工具，但随着群聊和联系人增多，用户面临普遍痛点：
- **信息过载**：工作群、家庭群、兴趣群消息999+，重要信息被淹没
- **语音不便**：语音消息在开会、通勤等场景无法收听，逐条转文字效率低
- **历史难查**：微信自带搜索能力有限，找半年前的消息如同大海捞针
- **监控缺失**：重要群的消息无法及时获知，错过 @自己 的通知
- **总结困难**：几百条消息需要人工梳理，耗时耗力

现有解决方案（如 itchat、wechaty、OpenClaw）普遍存在**封号风险高**、**功能有限**、**隐私不安全**等问题。

### 1.2 产品定位

**一个安全、只读、本地运行的微信信息管理助手。**

核心价值：
- **安全**：不注入、不重签；写操作仅限小号探索性开启（发消息/自动回复），主号只读，封号风险极低
- **高效**：查询、总结、监控、语音转文字一站式解决
- **私密**：所有数据本地处理，不上传云端

### 1.3 目标用户

| 用户类型 | 核心需求 |
|---------|---------|
| 上班族 | 工作群消息管理、重要消息不遗漏、聊天记录快速查找 |
| 群主/管理员 | 大群监控、消息总结、群成员管理 |
| 销售/商务 | 客户消息管理、语音转文字、历史记录检索 |
| 家长 | 家长群通知监控、重要消息提醒 |
| 项目负责人 | 多项目群并行管理、进展对比、话题梳理 |

### 1.4 产品目标

- **短期（v1.0）**：实现只读查询 + 语音转文字 + 基础监控，满足核心信息获取需求
- **中期（v2.0）**：完善监控推送 + AI 总结 + 飞书同步，形成完整的信息管理闭环
- **长期（v3.0）**：安全的写操作（发消息）+ 多平台支持（Windows）+ 多账号管理

---

## 二、功能需求

### 2.1 模块一：只读查询（P0，已完成）

#### 2.1.1 会话列表

**需求描述**：用户可以查看所有私聊和群聊的列表，包含会话名称、未读数、最后一条消息、最后消息时间。

**用户故事**：
> 作为用户，我想一眼看到所有会话的未读情况，这样我不用一个个群点进去就能知道哪些需要处理。

**验收标准**：
- [x] 支持列出所有会话（私聊+群聊）
- [x] 显示未读消息数
- [x] 显示最后一条消息摘要
- [x] 显示最后消息时间
- [x] 支持按未读数/时间排序

#### 2.1.2 聊天记录查询

**需求描述**：用户可以读取任意联系人/群聊的历史消息，支持按时间范围过滤。

**用户故事**：
> 作为用户，我想查看某个群最近一周的聊天记录，这样我能了解这段时间发生了什么。

**验收标准**：
- [x] 支持按会话名称查询
- [x] 支持按时间范围查询（--since/--until）
- [x] 支持自然语言时间解析（"最近一周""上个月""昨天"）
- [x] 消息类型完整显示（文字/图片/语音/视频/位置/链接/撤回）
- [x] 大消息量自动摘要（>100条触发分层处理）
- [x] 合并聊天记录 / 收藏卡片内的完整链接列表可全量提取（CLI 仅显示前 10 条，全量需直读 `message_0.db` 的 appmsg recorditem，方法见 [advanced-usage.md](../references/advanced-usage.md)「合并聊天记录全量提取」）

#### 2.1.3 全文搜索

**需求描述**：用户可以搜索所有聊天记录中的关键词，支持按群、时间、消息类型过滤。

**用户故事**：
> 作为用户，我想搜索"合同"相关的消息，并且只看某个客户群里的，这样我能快速找到需要的信息。

**验收标准**：
- [x] 支持关键词搜索
- [x] 支持按群过滤（--in 参数）
- [x] 支持按时间范围过滤
- [x] 支持按消息类型过滤
- [x] 搜索结果高亮显示匹配内容

#### 2.1.4 联系人与群成员

**需求描述**：用户可以查看好友列表和群成员列表。

**用户故事**：
> 作为用户，我想知道某个群里有哪些人，这样我能了解群的组成。

**验收标准**：
- [x] 支持列出所有联系人
- [x] 支持查看指定群的成员列表
- [x] 显示成员昵称、备注、微信号

#### 2.1.5 未读消息

**需求描述**：用户可以快速查看所有未读消息。

**用户故事**：
> 作为用户，我想一键看到所有未读消息，这样我不用一个个群翻。

**验收标准**：
- [x] 列出所有有未读消息的会话
- [x] 显示未读消息数量
- [x] 显示未读消息摘要

#### 2.1.6 统计信息

**需求描述**：用户可以查看消息量统计、活跃度分析。

**用户故事**：
> 作为用户，我想知道某个群这周发了多少消息、谁最活跃，这样我能了解群的情况。

**验收标准**：
- [x] 支持按时间范围统计消息量
- [x] 支持按发言人统计
- [x] 支持消息类型分布统计

#### 2.1.7 导出功能

**需求描述**：用户可以将聊天记录导出为 Markdown/JSON 文件。

**用户故事**：
> 作为用户，我想把重要的聊天记录导出备份，这样换手机也不会丢。

**验收标准**：
- [x] 支持导出为 Markdown
- [x] 支持导出为 JSON
- [x] 支持按会话/时间范围导出

### 2.2 模块二：语音转文字（P0，已完成）

#### 2.2.1 语音列表

**需求描述**：用户可以查看所有语音消息列表。

**用户故事**：
> 作为用户，我想看到所有语音消息的清单，这样我能选择要转写哪些。

**验收标准**：
- [x] 列出语音消息（时间、会话、大小）
- [x] 支持按时间排序
- [x] 支持按会话过滤

#### 2.2.2 语音转文字

**需求描述**：用户可以将语音消息转成文字，支持批量转写。

**用户故事**：
> 作为用户，我想把一个群里最近的语音都转成文字，这样我不用一条条听。

**验收标准**：
- [x] 支持单条转写
- [x] 支持批量转写（最近N条）
- [x] 支持按会话过滤
- [x] 支持按日期过滤
- [x] 中文识别准确率 >90%
- [x] 转写速度比实时快 5 倍以上
- [x] 支持文本和 JSON 两种输出格式

#### 2.2.3 技术约束

- 语音数据本地处理，不上传云端
- 支持微信 4.x 的 media_0.db 加密数据库
- media_0.db 密钥由 `wx init --force` 在微信运行中提取，无需播放语音；语音收到后自动下载入库，转写全程免播放（2026-09 验证，VoiceInfo 直读）

### 2.3 模块三：主动监控推送（P1，已完成）

#### 2.3.1 监控群配置

**需求描述**：用户可以配置需要监控的群，工具也可以智能推荐。

**用户故事**：
> 作为用户，我想选择监控哪些群，或者让工具推荐活跃的群，这样我只关注重要的群。

**验收标准**：
- [x] 支持手动添加/移除监控群
- [x] 支持 AI 推荐（按消息量/人数/活跃度排序）
- [x] 推荐流程：AI 推荐列表 → 用户输入序号选择
- [x] 配置持久化保存

#### 2.3.2 增量拉取

**需求描述**：工具定时检查监控群的新消息。

**用户故事**：
> 作为用户，我希望工具在后台自动检查新消息，不用我手动刷新。

**验收标准**：
- [x] 默认每 5 分钟轮询一次（可配置 1-30 分钟）
- [x] 基于上次检查时间增量拉取
- [x] 已推送消息 ID 记录，避免重复推送

#### 2.3.3 重要消息即时提醒

**需求描述**：重要消息（@我/@all/关键词）立即推送，普通消息攒批汇总。

**用户故事**：
> 作为用户，我希望有人 @我 时立即通知我，普通消息可以等晚上一起看总结。

**验收标准**：
- [x] @我/@all 消息即时推送（5分钟内）
- [x] 关键词匹配消息即时推送
- [x] 特定人消息可配置即时推送
- [x] 普通消息不推送，只进每日总结
- [x] 不重要消息（系统通知/群公告等）可配置忽略

> **实现状态（2026-09-16 核对）**：重要消息的**判定与分级逻辑已完成**（@我/@all → 关键词 → 特定人 → 系统消息 → 普通，见 wx-monitor.py）；但**实时外推到手机这一环尚未闭环**——豆包 MCP 主动冒泡通知经验证客户端不唤起、豆包定时任务最小间隔 900 秒不实时。已选定 **QQ 邮箱 SMTP** 作为外推通道（国内直连、稳定），**当前阻塞于用户未提供 16 位 SMTP 授权码**，`qq_mail_notify.py` 未落盘。在此之前，重要消息只能在豆包会话内主动查询或由每日总结体现。

#### 2.3.4 每日总结

**需求描述**：每天定时生成监控群消息汇总。

**用户故事**：
> 作为用户，我希望每天晚上收到一份"今天这些群都聊了啥"的总结，这样我不用自己翻。

**验收标准**：
- [x] 每日固定时间生成（默认 21:00，可配置）
- [x] 按群分组总结
- [x] 话题聚类
- [x] 重要消息标注
- [x] 支持在豆包内生成与查看
- [ ] 推送到飞书文档（`daily --sync` 仅占位未接线，见模块七）

#### 2.3.5 性能保护

**需求描述**：消息量过大时提醒用户，避免性能问题。

**验收标准**：
- [x] 单次轮询 >500 条提醒用户
- [x] 建议监控群 ≤20 个
- [x] 已推送 ID 最多保留 1000 条（自动清理）

#### 2.3.6 MCP 连接器（已完成，2026-09）

把监控/查询能力以 STDIO MCP 服务暴露给豆包桌面端，在连接器市场以"自定义连接器"安装。

- 服务：`mcp-server/server.py`（fastmcp，服务名 `wechat-monitor`），提供 5 个工具：`get_new_messages`、`get_monitor_status`、`mark_messages_as_read`、`update_config`、`get_message_history`。
- 配置（豆包桌面端「连接器·技能·伙伴」→「连接器」tab → 自定义，STDIO）：命令填 `mcp-server/.venv/bin/python` 的绝对路径，参数填 `server.py` 的绝对路径。
- 已修复：豆包 GUI（launchd）拉起连接器时 `PATH` 仅 `/usr/bin:/bin:/usr/sbin:/sbin`，`shutil.which("wx")` 会返回 None；现 `_resolve_wx_bin()` 按 which → `/usr/local/bin/wx`（Intel）→ `/opt/homebrew/bin/wx`（Apple Silicon）→ `wx` 回退。
- 重启方式：豆包连接器开关关→开（直接 kill 进程对 STDIO 托管进程无效）。
- 边界：MCP 工具为只读查询/状态管理；MCP 服务**主动 notification 冒泡**经验证客户端不唤起，故实时外推仍走 §2.3.3 的邮箱通道。

#### 2.3.7 合并聊天记录识别与拆分（监控子功能，待办）

**需求描述**：别人转发来的"合并聊天记录 / 收藏卡片"在工具里只是一条卡片、CLI 只显示前 10 条。需要**自动识别这类消息**，并把其中**每条子消息拆分还原**为结构化内容；实时监控收到时自动展开、纳入重要性判定与总结/提醒，历史查询也能对任意一/多条手动展开。

**用户故事**：
> 作为用户，有人甩给我一份上百条的合并聊天记录，我不用逐条点开，工具能直接把里面谁说了什么、什么时间、有哪些链接/图片拆出来并总结；监控群里出现合并记录时也不会被当成一条卡片漏掉。

**验收标准**：
- [ ] 识别合并记录类型消息（`local_type=81604378673`，appmsg recorditem），**不限会话、不限最新一条**
- [ ] 拆分每条子消息：发送人、时间、消息类型、文本内容、链接 URL
- [ ] 图片/语音/视频等子媒体可定位，并复用既有附件导出 / 语音转写链路处理
- [ ] 实时监控收到合并记录时自动展开，子消息纳入重要性判定与每日总结
- [ ] 历史查询可对指定的一/多条合并记录手动展开
- [ ] 通用：不局限于特定来源（如豆包链接），任意合并记录都能拆

> 现状（2026-09）：直读 `message_0.db` + zstd 解压 + 解析 recorditem 的路径已验证（文件传输助手两份记录合并去重 124 条链接，方法见 [advanced-usage.md](../references/advanced-usage.md) §5）；**通用拆分脚本未固化**。注意 CDATA 内 `<desc>` 只是前 5 条 + 字面 `...` 的截断摘要，完整子消息必须解析整条解压 XML，不能只取 desc。

### 2.4 模块四：AI 总结与分析（P1，已完成）

#### 2.4.1 聊天总结

**需求描述**：AI 自动总结某段时间的聊天内容。

**验收标准**：
- [x] 话题聚类
- [x] 重要消息识别
- [x] 摘要粒度可调（简版/详版）
- [x] 支持按时间范围总结

#### 2.4.2 多群对比

**需求描述**：同时对比多个群的进展。

**验收标准**：
- [x] 支持 2-5 个群对比
- [x] 对比维度：消息量、主要话题、重要事件、风险问题
- [x] 输出对比表格

#### 2.4.3 歧义处理

**需求描述**：群名/人名有歧义时，先列候选再确认，不猜测。

**验收标准**：
- [x] 检测到多个匹配时列出候选
- [x] 反问用户确认
- [x] 不自动选择第一个匹配

#### 2.4.4 大消息量处理

**需求描述**：几百上千条消息也能高效摘要。

**验收标准**：
- [x] 四级分层策略（<50条/50-200条/200-500条/>500条）
- [x] 避免 token 超限
- [x] 分批次处理

### 2.5 模块五：附件与媒体（P2，部分完成）

#### 2.5.1 图片解密导出

**需求描述**：微信图片 .dat 加密文件解密导出到本地。

**验收标准**：
- [x] 支持列出图片附件
- [x] 支持解密导出
- [x] 导出图片可正常打开

#### 2.5.2 位置消息解析

**需求描述**：位置消息 XML 解析，提取经纬度/地址/POI。

**验收标准**：
- [x] 解析位置消息 XML
- [x] 提取经纬度、地址、POI 名称

#### 2.5.3 视频消息（待实现）

**需求描述**：视频消息下载和内容理解。

**验收标准**：
- [ ] 支持视频文件下载
- [ ] 支持视频内容理解（需额外能力）

### 2.6 模块六：写操作（P2，开发中）

> 核心原则：只读为主，写操作为辅；人工审核，低频操作；先小号验证。
>
> **重要说明**：本模块中的"自动回复"功能为**探索性质**，本质上是客服（企业微信）场景更需要的能力，普通用户即使有需求也是轻需求。本阶段目标是**验证能做到什么程度**，不作为核心功能推广。自动回复仅在微信小号上测试，绝不在大号使用。

#### 2.6.1 发送文字消息（手动确认模式）

**需求描述**：用户说"给XX发消息：内容"，AI 起草，用户明确说"发送"后执行。

**用户故事**：
> 作为用户，我想对指定联系人/群发文字消息，不用切换到微信手动操作，但发送前必须确认，以免发错。

**验收标准**：
- [x] AI 解析接收人和内容，展示"接收人：XX，内容：XXX"
- [x] 用户必须明确说"发送"才执行；说"好""对""嗯"不发送
- [x] 接收人不存在时，AI 列出候选反问，不猜测不发送（发送前另有 OCR 聊天标题验证兜底）
- [x] 用户说"改一下：新内容"时，更新内容重新展示
- [x] 用户说"算了/取消"时，中止不发送
- [x] 用 UI 自动化操作微信官方界面（Cmd+F 键盘操作 + cliclick/OCR，见 SKILL §6 与 docs/phase3-write-spec.md）
- [x] 不注入微信进程、不调用私有 API、不重签微信
- [x] 操作间隔 >3 秒，不模拟人类无法达到的速度
- [x] 发送后立即提示"已发送，可在2分钟内撤回"

#### 2.6.2 发送图片/文件（后续阶段）

**验收标准**：
- [ ] 支持发送本地图片
- [ ] 支持发送本地文件
- [ ] 人工确认后发送

#### 2.6.3 @某人发消息（后续阶段）

**验收标准**：
- [ ] 支持在群里 @指定成员
- [ ] 人工确认后发送

#### 2.6.4 自动回复（探索性质，验证中）

> **定位说明**：本功能为探索性质，验证 AI 自动回复能做到什么程度。本质是客服/企业微信场景更需要，普通用户是轻需求。仅在小号测试。

**需求描述**：配置关键词/特定人/群触发，AI 自动生成回复，经安全过滤后自动发送，同时记录日志并通知用户。

**用户故事**：
> 作为用户，我想在临时离开时，对常见问题（如"在吗""价格"）自动回复，以免错过重要消息，但回复内容要安全，不能发不合适的内容。

**触发条件**：
- [x] 支持关键词触发（消息包含预设关键词）
- [x] 支持特定人触发（只对指定联系人自动回复）
- [x] 支持特定群触发（只对指定群自动回复）
- [x] 以上条件可组合配置

**内容生成与安全过滤**：
- [ ] AI 根据收到的消息内容自动生成回复（当前为模板回复，后续接入 AI 生成）
- [x] 10类黑名单过滤（金钱/隐私/承诺/敏感/辱骂/机密/专业建议/身份冒充/暴露AI/反问质疑）
- [x] 命中黑名单不发送，记录日志并通知用户"自动回复被拦截"
- [ ] 用户可选择手动回复或"强制发送"（不推荐）

**频率限制**：
- [x] 全局每分钟 ≤3 条
- [x] 全局每小时 ≤20 条
- [x] 同一个人/群 5分钟内 ≤2 条
- [x] 23:00-08:00 自动暂停（只记录不回复）
- [x] 达上限后暂停，通知用户，可手动说"继续自动回复"恢复

**日志与通知**：
- [x] 每次自动回复记录日志（时间/触发人/原始消息/回复内容/是否成功）
- [x] 每次自动回复后通知用户（摘要：时间/触发人/原始消息摘要/回复内容摘要）
- [x] 日志可查询，支持按时间/触发人筛选

**控制开关**：
- [x] 用户说"暂停自动回复"立即停止
- [x] 用户说"继续自动回复"恢复
- [ ] 用户说"关闭自动回复"关闭配置
- [x] 暂停/恢复/关闭状态持久化保存

**风控底线**：
- [x] 仅在微信小号上测试，绝不在大号使用
- [x] 不做批量发送、营销群发
- [x] 测试期间持续观察微信状态，出现异常立即停止
- [x] 不做绕过微信风控的操作

**明确不做（撤回相关）**：
- ❌ 不做工具代撤回（发送后仅提示用户 2 分钟内在微信手动撤回）。
- ❌ 不做"防撤回 / 阻止对方撤回 / 恢复已撤回正文"——稳定实现需注入或 hook 微信进程，属已验证会触发风控的高风险路径。
- 🟡 实时监控落盘的本地副本若**恰好**在对方撤回前已抓取，可在展示时标注"该消息后被撤回"，这只是只读存档的**偶然附带效果（best-effort），不承诺覆盖**：监控未运行、轮询间隔内撤回、媒体未下载完即撤回等情况都会漏。

### 2.7 模块七：飞书同步（P2，待实现）

#### 2.7.1 每日总结同步到飞书

> **账号与位置约束（2026-09 核对）**：必须使用**豆包账号"用户707217"**（手机号 17321237242 注册的豆包身份，非飞书直登手机号账号）；归档到飞书知识库 **AI 分类**（AI 知识库 → 豆包 → 微信自动化）。**不使用**"文档链接分享权限"方式。早期一次同步放错位置导致无访问权限，收尾时需在正确账号/空间重新落位。

**验收标准**：
- [ ] 同步到飞书知识库指定位置（豆包账号、AI 分类）
- [ ] 按日期组织文档
- [ ] 每天新建文档，不覆盖历史
- [ ] 支持手动同步和定时自动同步

---

## 三、非功能需求

### 3.1 安全需求

| 编号 | 需求 | 优先级 |
|------|------|--------|
| SEC-1 | 不修改微信数据库，纯只读操作 | P0 |
| SEC-2 | 不注入微信进程，不重签微信 | P0 |
| SEC-3 | 不自动发送消息，所有写操作需人工确认 | P0 |
| SEC-4 | 数据本地处理，不上传云端 | P0 |
| SEC-5 | 密钥提取使用关 SIP + sudo，不改变微信 code identity | P0 |
| SEC-6 | 写操作先在小号验证，确认安全后再考虑大号 | P1 |
| SEC-7 | 发送后观察微信状态，异常立即停止 | P1 |

### 3.2 性能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| PERF-1 | 单条语音转文字速度 | 比实时快 5 倍以上 |
| PERF-2 | 聊天记录查询响应 | < 3 秒（1000条以内） |
| PERF-3 | 监控轮询间隔 | 可配置 1-30 分钟，默认 5 分钟 |
| PERF-4 | 大消息量处理 | 支持单次 500+ 条消息摘要 |
| PERF-5 | 监控群数量 | 建议 ≤20 个，超过提醒 |

### 3.3 兼容性需求

| 编号 | 需求 | 状态 |
|------|------|------|
| COMP-1 | macOS Intel x86_64 | ✅ 已验证 |
| COMP-2 | macOS Apple Silicon | ✅ 理论支持（wx-cli 支持） |
| COMP-3 | 微信 4.1.8 | ✅ 已验证 |
| COMP-4 | Windows 平台 | ⏳ 已调研，待实测 |
| COMP-5 | 多微信版本适配 | ⏳ 待测试 |

### 3.4 易用性需求

| 编号 | 需求 |
|------|------|
| USE-1 | 自然语言交互，用户说"最近一周"自动解析时间 |
| USE-2 | 有歧义时先列候选再确认，不猜测 |
| USE-3 | 命令失败时给友好提示和解决建议 |
| USE-4 | 常用查询历史记录，支持快捷重复 |
| USE-5 | 配置可视化，不用改代码 |

---

## 四、用户流程

### 4.1 首次使用流程

```
用户开始
  ↓
关闭 SIP（用户操作，一次性）
  ↓
安装 wx-cli 和依赖（自动）
  ↓
提取数据库密钥（自动，需微信运行中）
  ↓
验证查询功能（自动测试）
  ↓
配置监控群（AI 推荐 + 用户选择）
  ↓
开始使用 ✅
```

### 4.2 日常使用流程

```
用户："我有哪些未读？"
  → 工具查询未读消息
  → 展示未读清单

用户："张三最近聊了什么？"
  → 工具查询聊天记录
  → AI 总结话题聚类
  → 展示总结

用户："把这段语音转成文字"
  → 工具提取语音
  → 解码 + 语音识别
  → 展示文字

用户："监控产品群，有 @我 就告诉我"
  → 工具添加监控配置
  → 后台定时轮询
  → 重要消息即时推送
```

---

## 五、版本规划

### v1.0（已完成）

- ✅ 只读查询（19个命令）
- ✅ 语音转文字
- ✅ AI 总结与分析
- ✅ 基础监控推送
- ✅ 图片解密导出

### v2.0（进行中）

- ✅ MCP 连接器（STDIO，5 个只读工具，GUI 精简 PATH 修复，2026-09）
- ✅ 多账号支持（单 App 切换 `wx-account.sh` + wx-cli 多账号源码改造；同机多开经验证不可行）
- ✅ 合并聊天记录 / 收藏卡片全量直读（绕过 CLI 前 10 条截断，2026-09）
- ✅ 写操作（发消息，小号验证；send_message.sh 7 步流程实测；自动回复框架已上、仅小号探索）
- 🔄 监控判定与每日总结已完成；**实时外推到手机未闭环**——QQ 邮箱 SMTP 通道待用户提供 16 位授权码（见 §2.3.3）
- ⏳ 飞书知识库同步（`daily --sync` 仅占位，脚本 TODO，未接线；账号/位置约束见模块七）

### v3.0（规划中）

- ⏳ Windows 平台适配（安装/使用说明、数据库与语音路径对应，未实测）
- ⏳ 视频内容理解（视频下载 + 内容理解）
- ⏳ 朋友圈/公众号深度查询
- ⏳ 数据备份与恢复
- ⏳ 写操作扩展：发送图片/文件、群里 @某人（W-2/W-3）

---

## 六、风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| 微信版本更新导致密钥提取失效 | 高 | 中 | 锁定微信版本，关闭自动更新；关注社区动态 |
| 微信风控封号 | 高 | 低 | 纯只读操作，不注入不重签；小号优先验证 |
| SIP 关闭带来系统安全风险 | 中 | 低 | 用户自行管理 SIP 开关，提取密钥后可开启 |
| 语音识别准确率不足 | 中 | 低 | 使用 SenseVoiceSmall 模型，中文准确率高；持续优化 |
| 大消息量导致性能问题 | 中 | 中 | 四级分层处理，分批摘要，性能保护提醒 |

---

## 七、成功指标

| 指标 | 目标 |
|------|------|
| 核心查询功能可用性 | >99% |
| 语音转文字准确率 | >90%（中文） |
| 重要消息推送延迟 | <5 分钟 |
| 用户首次配置时间 | <10 分钟 |
| 封号事件 | 0 |
| 用户满意度（NPS） | >50 |
